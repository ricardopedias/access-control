<?php

if (! function_exists('render_form_permissions')) {

    function render_form_permissions($component)
    {
        // return (old($key, null) == $active_value || $default == $active_value)
        //     ? 'checked' : '';
    }
}

